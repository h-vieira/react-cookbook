import { Link } from 'react-router-dom'
const Library = () =>{

    return (
        <React.Fragment>
            <Link to="/"> Home </Link>
        </React.Fragment>
    )
};

export default Library;